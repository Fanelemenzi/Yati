import 'package:flutter/material.dart';

class MostUsedPage extends StatelessWidget {
  const MostUsedPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("This is the most used page")),
    );
  }
}